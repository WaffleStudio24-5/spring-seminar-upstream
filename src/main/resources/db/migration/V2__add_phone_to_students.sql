-- 요구사항이 추가되어 학생의 전화번호를 저장해야 한다.
--
-- 혼자 개발한다면 자기 DB 에 ALTER 를 한 번 치면 끝이지만, 그러면 팀원들의 로컬 DB 와
-- 운영 DB 에도 같은 문을 기억했다가 실행해 주어야 한다.
-- 그래서 스키마 변경을 소스코드로 남긴다. 이 파일을 pull 받은 사람은 애플리케이션을
-- 켜는 것만으로 같은 스키마가 된다.
--
-- 이미 등록된 학생에게는 전화번호가 없으므로 NULL 을 허용한다.

ALTER TABLE students ADD COLUMN phone VARCHAR(20) NULL;
